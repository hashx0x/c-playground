#include <iostream>
#include <string>
#include <vector>

using namespace std;

string solution(string code) {
  int mode = 0;

  for (int idx = 0; idx < code.size(); idx++) {
    }
  string answer = "";
  return answer;
}